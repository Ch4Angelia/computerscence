name: Daily Food Data Update

on:
  schedule:
    - cron: '0 4 * * *'
  
  workflow_dispatch:

  repository_dispatch:
    types: [trigger_update]

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas deep-translator requests

    - name: Run update script
      run: python update_data.py

    - name: Commit and push changes
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "actions@github.com"
        
        # 修改這裡：加入正確路徑的檔案
        git add Sedentary_Lifestyle_Management/food_database.json
        
        # 如果有變動才 commit
        git commit -m "Auto-update food database" || echo "No changes to commit"
        git push